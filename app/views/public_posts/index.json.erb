<%
  values = {
    html: render(partial: 'post', format: :html, collection: @posts),
    url: root_path(page: @next_page)
  }
  values[:lastPage] = true if @next_page > @posts.num_pages
%>
<%= values.to_json.html_safe %>
